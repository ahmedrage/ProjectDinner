using UnityEngine;
using System.Collections;

public class canvasRotationScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//transform.rotation = new Quaternion (transform.rotation.x, transform.rotation.y, Quaternion.identity.z, 1);


	}
	
	// Update is called once per frame
	void Update () {
	}
}
